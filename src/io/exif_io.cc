#ifndef exif_io_h
#define exif_io_h

#include "io/exif_io.h"

using std::string;

namespace open3DCV
{
    int read_exif(const string fname, float focal_length)
    {
        
        return 0;
    }
}

#endif
